SEQ replaceIfWindow(size: number, step: number, fn: ArrayFinderFn<ET>, rep: Replacer<ET[], ET>): this {
 - ???
SEQ replaceIfReverseWindow(size: number, step: number, fn: ArrayFinderFn<ET>, rep: Replacer<ET[], ET>): this {
 - ???

COL protected replaceRelative(pos: SeqIndices, rep: Replacer<T, T>, len: number, offset: number): this {
COL insertBefore(pos: SeqIndices, rep: Replacer<T, T>): this {
 - need a mapAndInsertBefore() and a flatMapAndInsertBefore()
COL insertAfter(pos: SeqIndices, rep: Replacer<T, T>): this {
 - need a mapAndInsertAfter() and a flatMapAndInsertBefore()
COL replaceIndices(pos: SeqIndices, rep: Replacer<T, T>): this {
 - already has a mapIndices()/flatMapIndices()
COL replaceFirstIndex(fn: FinderFn<T>, rep: Replacer<T, T>): this {
 - already has a mapFirstIndex()/flatMapFirstIndex()
COL replaceLastIndex(fn: FinderFn<T>, rep: Replacer<T, T>): this {
 - already has a mapLastIndex()/flatMapLastIndex()
COL replaceIf(fn: FinderFn<T>, rep: Replacer<T, T>): this {
 - already has a mapIf()/flatMapIf()
COL replaceNth(n: number, rep: Replacer<T, T>, offset = 0): this {
 - already has a mapNth()/flatMapNth()
COL replaceSlice(start: number, end: number, rep: Replacer<this, T>): this {
 - already has a mapSlice()/flatMapSlice()
